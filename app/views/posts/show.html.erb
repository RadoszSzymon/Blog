<div class="post_content">
  <%= image_tag @post.image.url(:medium) %>
	<h1 class="title"><%= @post.title %></h1>
	
	<div class="without_image">
		<p class="date">
			Dodano <%= @post.created_at.strftime("%d.%m.%Y") %> 
			<% if user_signed_in? %>
			| <%= link_to 'Edytuj', edit_post_path(@post) %> |
			  <%= link_to 'Usuń', post_path(@post), method: :delete, data: { confirm: 'Jesteś pewien?' } %>
		  <% end %>
		</p>
		<p class="body"><%= @post.body %></p>
	
		<div id="comments">
			<h2>Liczba komentarzy: <%= @post.comments.count %></h2>
			<hr>
			<%= render @post.comments %>
	
			<h3>Dodaj komentarz:</h3>
			<%= render "comments/form" %>
		</div>
	</div>

</div>
